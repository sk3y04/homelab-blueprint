# ==========================================================================
# Matrix Synapse + Element Web — Environment Variables
# ==========================================================================
# Copy this file to .env and fill in your own values:
#   cp .env.example .env
# ==========================================================================

# ── Timezone ──────────────────────────────────────────────────────────────
TZ=America/New_York

# ── Synapse ───────────────────────────────────────────────────────────────
# Host port Synapse listens on (Nginx proxies to this via VPN tunnel).
SYNAPSE_HTTP_PORT=8008

# Host directory for Synapse persistent data (homeserver.yaml, signing
# keys, media_store, uploads, etc.). Created by prepare.sh.
SYNAPSE_DATA_DIR=/path/to/synapse/data

# ── PostgreSQL (Synapse DB) ───────────────────────────────────────────────
SYNAPSE_POSTGRES_DB=synapse
SYNAPSE_POSTGRES_USER=synapse
SYNAPSE_POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# Host directory for PostgreSQL data files.
SYNAPSE_DB_DIR=/path/to/synapse/db

# ── Element Web ───────────────────────────────────────────────────────────
# Host port Element Web listens on (Nginx proxies to this via VPN tunnel).
ELEMENT_HTTP_PORT=8088

# Host directory containing Element Web config.json.
# Created by prepare.sh — edit config.json inside to customise branding.
ELEMENT_CONFIG_DIR=./element
