# TriliumNext Notes configuration (example)
# Copy to .env and adjust.

TZ=UTC

# Ports
TRILIUM_HTTP_PORT=8085

# Volumes — path on the host for persistent note data, config, and backups.
TRILIUM_DATA_DIR=/path/to/trilium/data
