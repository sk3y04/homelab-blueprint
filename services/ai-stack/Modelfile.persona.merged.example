# ==========================================================================
# Ollama Modelfile — Merged Persona GGUF Model
# ==========================================================================
# This Modelfile loads a full merged GGUF model that already contains the
# persona fine-tuning weights. Use this path when Ollama cannot run ADAPTER
# directives at inference time.
#
# Prerequisites:
#   1. Merge the LoRA adapter into the base model.
#   2. Export the merged full model to GGUF.
#   3. Place the merged GGUF file at:
#      /opt/ai-stack/data/merged-models/persona-merged.gguf
#   4. Create the model in Ollama:
#      docker exec ai-ollama ollama create persona -f /models/Modelfile.persona.merged
# ==========================================================================

FROM /merged-models/persona-merged.gguf

TEMPLATE """{{- if .Messages -}}
{{- range .Messages }}<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{- end }}<|im_start|>assistant
{{- else -}}
{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{- end }}<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
{{- end -}}"""

PARAMETER temperature 0.7
PARAMETER top_p 0.85
PARAMETER top_k 40
PARAMETER repeat_penalty 1.15
PARAMETER num_ctx 8192
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"

SYSTEM """
Reply in the learned persona from the fine-tuning data.

Keep the voice natural, specific, and conversational rather than formal or
generic. Prefer short direct answers over structured help menus. Do not add
capability disclaimers, safety boilerplate, or generic assistant framing unless
the user explicitly asks about your limits or the topic truly requires it.

Stay in-character, answer in the same language as the user unless there is a
reason to switch, and stop cleanly when the reply is complete.
"""