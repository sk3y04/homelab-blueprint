# ==========================================================================
# AI Stack — Environment Variables
# ==========================================================================
# Copy this file to .env and fill in the values before starting:
#   cp .env.example .env
#
# Secrets in this file MUST NOT be committed to version control.
# ==========================================================================

# ── General ───────────────────────────────────────────────────────────────
TZ=America/New_York

# ── Data Directory ────────────────────────────────────────────────────────
# Base path for all persistent AI stack data (models, chat history, metrics).
# Ensure this directory exists and has enough space (100 GB+ recommended for
# multiple models). Do NOT place on a tmpfs or volatile mount.
AI_DATA_DIR=/opt/ai-stack/data

# ── Ollama ────────────────────────────────────────────────────────────────
OLLAMA_PORT=11434
# Max models loaded in VRAM simultaneously. 1 is safest for 24 GB with 32B models.
OLLAMA_MAX_LOADED_MODELS=1
# Parallel request slots. 2 is conservative for 32B models on 24 GB VRAM.
OLLAMA_NUM_PARALLEL=2

# ── Open WebUI ────────────────────────────────────────────────────────────
OPEN_WEBUI_PORT=8080
# Secret key for session encryption. Generate with:
#   openssl rand -hex 32
OPEN_WEBUI_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32
# Set to true for first startup to create admin account, then set to false.
OPEN_WEBUI_ENABLE_SIGNUP=true

# ── OpenClaw ──────────────────────────────────────────────────────────────
OPENCLAW_PORT=8081
# Docker image — VERIFY this is correct before first startup.
# Run: docker search openclaw
OPENCLAW_IMAGE=openclaw/openclaw:latest
# Default model for agent tasks.
OPENCLAW_DEFAULT_MODEL=qwen3:32b

# ── DCGM Exporter ────────────────────────────────────────────────────────
DCGM_EXPORTER_PORT=9400
